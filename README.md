# nextpost

## Local setup

```bash
npm install
npm run dev
```

## GitHub

Vercel deployment is deferred. For now, connect this project to GitHub only:

```bash
git init
git add .
git commit -m "Initial NEXTPOST project"
git branch -M main
git remote add origin <GITHUB_REPOSITORY_URL>
git push -u origin main
```
